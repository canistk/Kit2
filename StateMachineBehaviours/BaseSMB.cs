using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Kit2.SMB
{
    public class BaseSMB : StateMachineBehaviour
	{
		//public override void OnStateEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
		//{
		//	base.OnStateEnter(animator, stateInfo, layerIndex);
		//	var transitions			= animator.GetAnimatorTransitionInfo(layerIndex);
			
		//	var currStateInfo	= animator.GetCurrentAnimatorStateInfo(layerIndex);
		//	var currClipInfoCnt = animator.GetCurrentAnimatorClipInfoCount(layerIndex);
		//	var currClipInfo	= animator.GetCurrentAnimatorClipInfo(layerIndex);

		//	var nextStateInfo	= animator.GetNextAnimatorStateInfo(layerIndex);
		//	var nextClipInfoCnt	= animator.GetNextAnimatorClipInfoCount(layerIndex);
		//	var nextClipInfo	= animator.GetNextAnimatorClipInfo(layerIndex);

		//}
	}
}
